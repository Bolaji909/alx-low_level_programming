#include "main.h"

/**
 * _islower - Chexks lower case characters
 * @c: The character needs to be checked
 * Rerurn: 1 for lowercase character or 0 otherwise
 */

int _islower(int c)

{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
